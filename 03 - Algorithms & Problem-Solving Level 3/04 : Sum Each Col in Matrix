// Problem 4.

#include <iostream>   
#include <string>   
#include <iomanip>
#include <vector>
#include <cctype>
#include <fstream>
#include <ctime>
#include <cstdlib>

using namespace std;  

int RandomNumber(int From, int To)
{
 int randNum = rand() % (To - From + 1) + From;

 return randNum;
}

void FillMatrixWithRandomNumbers(int Matrix[3][3], int Rows, int Columns)
{
 for (int i = 0; i <= Rows - 1; i++)
 {
  for (int j = 0; j <= Columns - 1; j++)
  {
   Matrix[i][j] = RandomNumber(1, 100);
  }
 }
}

void PrintMatrix(int Matrix[3][3], int Rows, int Columns)
{
 for (int i = 0; i <= Rows - 1; i++)
 {
  for (int j = 0; j <= Columns - 1; j++)
  {
   cout << setw(3) << Matrix[i][j] << " ";
  }

  cout << endl;
 }
}

int SumCol(int arr[3][3], int Rows, int NumberOfcolumn)
{
 int Sum = 0;

 for (int i = 0; i <= Rows - 1; i++)
 {
  Sum = Sum + arr[i][NumberOfcolumn];
 }

 return Sum;
}

void PrintEachColumnSum(int arr[3][3], short Columns,short Rows)
{
 cout << "\n The the following are the sum of each col in the matrix:\n";
 for (int i = 0; i <= Columns - 1; i++)
 {
  cout << " Col " << i + 1 << " Sum = " << SumCol(arr, Rows, i) << endl;
 }

 cout << endl;
}


int main()
{
 srand((unsigned)time(NULL));

 int arr[3][3];

 FillMatrixWithRandomNumbers(arr, 3, 3);

 cout << " The following is a 3x3 random matrix : \n";
 PrintMatrix(arr, 3, 3);

 PrintEachColumnSum(arr, 3, 3);
 
 return 0;
}
